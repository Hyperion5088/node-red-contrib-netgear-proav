# NETGEAR Pro AV Node-RED Tasks

Public-safe working list for the Node-RED node package.

1. Validate the live port-ID dropdown against each switch model in the lab.
2. Add direct switch discovery or subnet scan helper nodes after the shared-connection model is validated.
3. Add higher-level output shaping for common dashboard payloads such as port summary, PoE summary, LLDP summary, and VLAN membership.
4. Add optional protection helpers for critical ports before exposing easy toggle-style controls.
5. Add automated tests around request signing, session renewal, response parsing, and control payload generation.
6. Add screenshots and example flows before publishing to npm or flows.nodered.org.
